name: create-release-prs
version: 1
interface:
  display_name: Create Release PRs
  short_description: Open release/<version> PRs into main and develop. No merge, no tag.
  default_prompt: Create release PRs <version>
